import React from "react";
import ReactDOM from "react-dom";
import App from "./app"; // Importando seu App.js

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
